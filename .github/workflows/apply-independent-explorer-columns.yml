name: Apply Independent Explorer Columns

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Temporary implementation workflow retired"
