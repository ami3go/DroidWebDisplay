# v0.11.6 release verification

CI-only marker used to run the full release gate against the v0.11.6 release candidate. It does not affect application runtime or packaging inputs.

Rerun after aligning the final Ctrl+Enter clipboard test contract.
